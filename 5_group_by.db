SELECT
    product_category_name,
    MAX(product_weight_g) as mais_pesado,
    MIN(product_weight_g) as mais_leve,
    AVG(product_weight_g) as media_peso
FROM
    tb_products
WHERE
    product_category_name is NOT NULL
GROUP BY
    product_category_name;